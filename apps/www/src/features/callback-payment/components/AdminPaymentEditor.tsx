"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import { formatWon, paymentProducts, paymentStatusLabels, serviceStatusLabels, type PaymentProductCode, type PaymentStatus, type ServiceStatus } from "../domain";

type Payment = {
  id: number; version: number; productCode: string; productName: string; supplyAmount: number; vatAmount: number; totalAmount: number;
  paymentStatus: string; paymentDueAt: string | null; instructionEmailStatus: string; confirmationEmailStatus: string | null;
  evidenceType: string; evidenceStatus: string; serviceStatus: string; consultationStartAt: string | null;
  refundFinalAmount: number | null; refundReasonCode: string | null;
};

const CLIENT_LOADED_AT = Date.now();

export default function AdminPaymentEditor({ callbackId, payment }: { callbackId: number; payment: Payment | null }) {
  const router = useRouter();
  const [productCode, setProductCode] = useState<PaymentProductCode>("youth_integrated");
  const [depositorName, setDepositorName] = useState("");
  const [consultationStartAt, setConsultationStartAt] = useState("");
  const [refundReason, setRefundReason] = useState("before_registration");
  const [refundAdjustment, setRefundAdjustment] = useState("0");
  const [missingHalfHours, setMissingHalfHours] = useState("1");
  const [refundNote, setRefundNote] = useState("");
  const [message, setMessage] = useState<string | null>(null);
  const [busy, setBusy] = useState(false);

  async function request(path: string, method: "POST" | "PATCH", body: Record<string, unknown> = {}) {
    setBusy(true); setMessage(null);
    const response = await fetch(`/api/admin/callbacks/${callbackId}/payment${path}`, { method, headers: { "Content-Type": "application/json" }, body: JSON.stringify(body) });
    const result = await response.json().catch(() => ({})) as { error?: string };
    setBusy(false);
    setMessage(response.ok ? "처리했습니다." : result.error === "invalid_state" ? "현재 단계에서는 처리할 수 없습니다." : result.error === "invalid_request" ? "입력 내용을 확인해 주세요." : "서버에서 처리하지 못했습니다.");
    if (response.ok) router.refresh();
    return response.ok;
  }

  if (!payment) return <section className="rounded-2xl border border-navy/10 bg-white p-6"><h2 className="text-lg font-black">유료 통합 패키지</h2><p className="mt-2 text-sm leading-6 text-navy/55">20분 콜백 후 적합한 상품을 선택하면 72시간 유효한 무통장입금 안내를 보냅니다.</p><div className="mt-5 grid gap-3">{Object.values(paymentProducts).map((product) => <label key={product.code} className="flex cursor-pointer gap-3 rounded-xl border border-navy/10 p-4"><input type="radio" checked={productCode === product.code} onChange={() => setProductCode(product.code)} /><span><strong className="block">{product.name} · {formatWon(product.totalAmount)}</strong><span className="text-xs text-navy/50">{product.eligibility} · 공급가 {formatWon(product.supplyAmount)} + VAT {formatWon(product.vatAmount)}</span></span></label>)}</div><label className="mt-4 grid gap-2 text-sm font-bold">입금자명(선택)<input value={depositorName} onChange={(event) => setDepositorName(event.target.value)} maxLength={100} className="h-11 rounded-xl border border-navy/15 px-4" /></label><button disabled={busy} onClick={() => request("", "POST", { productCode, depositorName })} className="mt-5 h-11 rounded-xl bg-navy px-5 text-sm font-black text-white disabled:opacity-40">입금 안내 이메일 보내기</button>{message ? <p className="mt-4 rounded-xl bg-teal/10 p-3 text-sm font-bold">{message}</p> : null}</section>;

  const due = payment.paymentDueAt ? new Date(payment.paymentDueAt) : null;
  const overdue = payment.paymentStatus === "awaiting_payment" && Boolean(due && due.getTime() < CLIENT_LOADED_AT);
  const nextService: Partial<Record<ServiceStatus, ServiceStatus>> = { not_issued: "link_issued", link_issued: "registered", registered: "assessment_in_progress", assessment_in_progress: "assessment_completed", assessment_completed: "consultation_scheduled", consultation_scheduled: "consultation_completed" };
  const next = nextService[payment.serviceStatus as ServiceStatus];
  return <section className="rounded-2xl border border-navy/10 bg-white p-6"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-lg font-black">유료 서비스 관리</h2><p className="mt-1 text-xs text-navy/45">결제 버전 {payment.version}</p></div><span className={`rounded-full px-3 py-2 text-xs font-black ${overdue ? "bg-red-100 text-red-700" : "bg-teal/10"}`}>{overdue ? "입금기한 초과" : paymentStatusLabels[payment.paymentStatus as PaymentStatus] ?? payment.paymentStatus}</span></div><div className="mt-5 rounded-xl bg-cream p-4 text-sm leading-7"><strong>{payment.productName}</strong><br />공급가 {formatWon(payment.supplyAmount)} + VAT {formatWon(payment.vatAmount)} = <strong>{formatWon(payment.totalAmount)}</strong>{due ? <><br />기한 {due.toLocaleString("ko-KR", { timeZone: "Asia/Seoul" })}</> : null}</div><div className="mt-4 flex flex-wrap gap-2">{payment.paymentStatus === "awaiting_payment" ? <><button disabled={busy} onClick={() => request("/resend", "POST")} className="rounded-xl border border-navy/15 px-4 py-3 text-sm font-bold">입금 안내 재발송</button><button disabled={busy} onClick={() => request("/confirm", "POST")} className="rounded-xl bg-navy px-4 py-3 text-sm font-bold text-white">입금 확인</button><button disabled={busy} onClick={() => request("/cancel", "POST")} className="rounded-xl border border-red-200 px-4 py-3 text-sm font-bold text-red-700">입금 안내 취소</button></> : null}</div><p className="mt-3 text-xs text-navy/45">안내 이메일: {payment.instructionEmailStatus} · 확인 이메일: {payment.confirmationEmailStatus ?? "미발송"}</p>{payment.paymentStatus === "paid" ? <><div className="mt-6 border-t border-navy/10 pt-5"><h3 className="font-black">증빙</h3><div className="mt-3 flex flex-wrap gap-2"><button disabled={busy} onClick={() => request("/evidence", "PATCH", { evidenceType: "cash_receipt", evidenceStatus: "issued" })} className="rounded-xl border border-navy/15 px-3 py-2 text-sm font-bold">현금영수증 발행 완료</button><button disabled={busy} onClick={() => request("/evidence", "PATCH", { evidenceType: "tax_invoice", evidenceStatus: "issued" })} className="rounded-xl border border-navy/15 px-3 py-2 text-sm font-bold">세금계산서 발행 완료</button></div><p className="mt-2 text-xs text-navy/45">{payment.evidenceType} · {payment.evidenceStatus}</p></div><div className="mt-6 border-t border-navy/10 pt-5"><h3 className="font-black">평가·3시간 컨설팅</h3><p className="mt-2 text-sm">현재: {serviceStatusLabels[payment.serviceStatus as ServiceStatus] ?? payment.serviceStatus}</p>{next === "consultation_scheduled" ? <input type="datetime-local" value={consultationStartAt} onChange={(event) => setConsultationStartAt(event.target.value)} className="mt-3 h-11 rounded-xl border border-navy/15 px-3" /> : null}{next ? <button disabled={busy || (next === "consultation_scheduled" && !consultationStartAt)} onClick={() => request("/service", "PATCH", { serviceStatus: next, consultationStartAt: next === "consultation_scheduled" ? new Date(consultationStartAt).toISOString() : undefined })} className="mt-3 block rounded-xl bg-teal px-4 py-3 text-sm font-black text-white">다음 단계: {serviceStatusLabels[next]}</button> : null}</div><div className="mt-6 border-t border-navy/10 pt-5"><h3 className="font-black">환불</h3><select value={refundReason} onChange={(event) => setRefundReason(event.target.value)} className="mt-3 h-11 rounded-xl border border-navy/15 px-3 text-sm"><option value="before_registration">본부 등록 전 14일 이내</option><option value="consultation_cancelled">컨설팅 취소</option><option value="no_show">48시간 이내 취소·노쇼</option><option value="provider_unavailable">제공자 사유 미제공</option></select>{refundReason === "provider_unavailable" ? <label className="mt-3 grid gap-1 text-xs font-bold">미제공 30분 단위(1~6)<input type="number" min="1" max="6" value={missingHalfHours} onChange={(event) => setMissingHalfHours(event.target.value)} className="h-10 rounded-xl border border-navy/15 px-3" /></label> : null}<label className="mt-3 grid gap-1 text-xs font-bold">운영자 조정액(차감은 음수)<input type="number" value={refundAdjustment} onChange={(event) => setRefundAdjustment(event.target.value)} className="h-10 rounded-xl border border-navy/15 px-3" /></label><label className="mt-3 grid gap-1 text-xs font-bold">조정·환불 사유 메모<textarea value={refundNote} onChange={(event) => setRefundNote(event.target.value)} maxLength={500} rows={3} className="rounded-xl border border-navy/15 p-3" /></label><p className="mt-2 text-xs text-navy/45">환불계좌·주민번호·카드정보는 메모에 기록하지 마세요.</p><button disabled={busy} onClick={() => request("/refund", "POST", { action: "request", reason: refundReason, adjustmentAmount: Number(refundAdjustment), providerMissingHalfHours: Number(missingHalfHours), note: refundNote })} className="mt-3 rounded-xl border border-red-200 px-4 py-3 text-sm font-bold text-red-700">환불액 계산 및 접수</button></div></> : null}{payment.paymentStatus === "refund_pending" ? <button disabled={busy} onClick={() => request("/refund", "POST", { action: "complete" })} className="mt-5 rounded-xl bg-red-700 px-4 py-3 text-sm font-black text-white">수동 이체 후 환불 완료</button> : null}{payment.refundFinalAmount != null ? <p className="mt-3 text-sm font-bold">환불액 {formatWon(payment.refundFinalAmount)}</p> : null}{message ? <p role="status" className="mt-4 rounded-xl bg-teal/10 p-3 text-sm font-bold">{message}</p> : null}</section>;
}
